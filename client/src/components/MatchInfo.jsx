import Wrapper from "../assets/wrappers/MatchInfo";

const MatchInfo = ({ icon, text }) => {
  return (
    <Wrapper>
      <span className="job-icon">{icon}</span>
      <span className="job-text">{text}</span>
    </Wrapper>
  );
};

export default MatchInfo;
